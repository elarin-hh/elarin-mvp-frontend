# ============================================
# ELARIN FRONTEND - Environment Variables
# ============================================

# Backend API - NestJS + Fastify
# The backend runs on port 3001 (NestJS + Fastify)
# Endpoints: /auth, /exercises, /training, /gyms
# Em produção, use HTTPS para permitir cookies SameSite=None
VITE_API_BASE_URL=http://localhost:3001
# VITE_API_BASE_URL=https://api.suaempresa.com

# Supabase (opcional - usado pelo backend, não diretamente pelo frontend)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Base path (opcional) se o app for servido em um subpath do CloudFront
# Exemplo: /app
BASE_PATH=
