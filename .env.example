# ============================================
# ELARIN FRONTEND - Environment Variables
# ============================================

# Backend API - NestJS + Fastify
# The backend runs on port 3001 (NestJS + Fastify)
# Endpoints: /auth, /exercises, /training, /gyms
# In production, always use HTTPS to allow SameSite=None cookies
VITE_API_BASE_URL=https://api.suaempresa.com
# For local development, point to your local server:
# VITE_API_BASE_URL=http://localhost:3001

# Supabase (optional - only if frontend talks directly to Supabase)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Base path (optional) if the app is served from a subpath (e.g., via CloudFront)
# Example: /app
BASE_PATH=

# Force browser upgrade to HTTPS (set true when serving behind CloudFront/HTTPS; keep false only if hosting via plain HTTP)
FORCE_HTTPS=true
